# Supabase (ClearStack project siaktnkaavgefjxgprrf)
SUPABASE_URL=https://siaktnkaavgefjxgprrf.supabase.co
VITE_SUPABASE_URL=https://siaktnkaavgefjxgprrf.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-side only — never expose to browser (Vercel env)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# After changing dashboard source, rebuild admin assets:
#   npm run build:admin
# Or on Vercel, set VITE_* vars and use: npm run vercel-build:full
